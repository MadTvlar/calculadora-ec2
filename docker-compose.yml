version: "3.8"

services:
  calculadora:
    image: calculadora-ec2:${DOCKER_NAME} # ⬅️ Nome fixo da imagem
    build: .
    ports:
      - "${DOCKER_PORT}:8080"
    restart: always
    env_file:
      - .env
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/health"]
      interval: 10s
      retries: 3
